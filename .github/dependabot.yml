version: 2
updates:
  # Go modules - Security updates only
  - package-ecosystem: "gomod"
    directory: "/"
    schedule:
      interval: "daily"
      time: "09:00"
    open-pull-requests-limit: 5
    assignees:
      - "sukhera"
    commit-message:
      prefix: "security"
      include: "scope"
    labels:
      - "security"
      - "dependencies"
      - "go"
    # Only create PRs for security updates
    ignore:
      # Ignore all regular version updates, only allow security updates
      - update-types: ["version-update:semver-patch", "version-update:semver-minor", "version-update:semver-major"]

  # Frontend dependencies - Security updates only
  - package-ecosystem: "npm"
    directory: "/web"
    schedule:
      interval: "daily"
      time: "09:00"
    open-pull-requests-limit: 5
    assignees:
      - "sukhera"
    commit-message:
      prefix: "security"
      include: "scope"
    labels:
      - "security"
      - "dependencies"
      - "frontend"
    ignore:
      # Ignore all regular version updates, only allow security updates
      - update-types: ["version-update:semver-patch", "version-update:semver-minor", "version-update:semver-major"]

  # GitHub Actions - Security updates only
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "daily"
      time: "09:00"
    open-pull-requests-limit: 3
    assignees:
      - "sukhera"
    commit-message:
      prefix: "security"
      include: "scope"
    labels:
      - "security"
      - "dependencies"
      - "ci"
    ignore:
      # Ignore all regular version updates, only allow security updates
      - update-types: ["version-update:semver-patch", "version-update:semver-minor", "version-update:semver-major"]

  # Docker dependencies - Security updates only
  - package-ecosystem: "docker"
    directory: "/"
    schedule:
      interval: "daily"
      time: "09:00"
    open-pull-requests-limit: 3
    assignees:
      - "sukhera"
    commit-message:
      prefix: "security"
      include: "scope"
    labels:
      - "security"
      - "dependencies"
      - "docker"
    ignore:
      # Ignore all regular version updates, only allow security updates
      - update-types: ["version-update:semver-patch", "version-update:semver-minor", "version-update:semver-major"]